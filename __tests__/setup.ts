/**
 * Global test setup file for component tests.
 *
 * Configures @testing-library/jest-dom matchers so that every test file
 * can use `.toBeInTheDocument()`, `.toHaveClass()`, etc. without explicit imports.
 *
 * Preloaded via bunfig.toml [test].preload.
 */
import '@testing-library/jest-dom'
